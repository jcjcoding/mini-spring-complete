（1）设置过期时间（必做！）
不要让 Redis 存满数据，给缓存设置过期时间（比如 1 小时），自动清理不用的数据：

（2）缓存穿透（防坑！）
问题：请求一个不存在的数据（比如 id=-1 的商品），Redis 查不到就去查 MySQL，黑客会用这种方式攻击，打垮 MySQL。解决：给不存在的数据也存一个 “空值” 到 Redis，设置短过期时间：

（3）缓存更新（保证数据一致性）
问题：MySQL 数据更新后，Redis 缓存还是旧数据。解决：更新 MySQL 的同时，删除 Redis 缓存（最简单有效）：

读的时候 优先redis 如果redis没有去sql找并写到redis里面
写的时候 直接sql 然后删redis 