序列化 将内存里的对象转成存储器的文件
反序列化 将存储器文件转回内存的对象   

结果 持久化 和 跨设备

重点 写的东西没变但是读的方式变了，导致默认序列号变了，产生错误
解决 自定义序列号

```java

@Data
public class Result<T> implements Serializable {
    // 序列化版本号
    @Serial
    private static final long serialVersionUID = 1L;